<i class="icon-refresh"></i> <p class="author"> <%= link_to a.owner.person, profile_path(a.owner.person.nick) if a.owner %> </p> atualizou 

<% if activity.trackable %>
  <%= link_to activity.trackable.title, activity.trackable %>
<% else %>
  um projeto que não existe mais.
<% end %>