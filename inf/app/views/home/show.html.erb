<div class="homepage">

	<% if @conversations %>
		<h2> Messages </h2>
		<ul>
			<% @conversations.each do |c| %>
				<li>
					<%= c.is_read?(current_user) ? "(Lida)" : "(Nova!)" %>
					<% receipt = c.receipts_for current_user %>
					<ul>
						<% receipt.each do |rec| %>
							<li>
								<% msg = rec.message %>
								<%= "#{msg.sender.name} - #{msg.subject}: #{msg.body}" %>
							</li>
						<% end %>
					</ul>
				</li>
			<% end %>
		</ul>
	<% end %>

	<div class="center hero-unit">
		
	    <div class="row-fluid">
		    
		    <div class="span5">
		    	<img src="/assets/logo.png">
		    </div>
			
			<div class="span7">
				<!--<img src="/assets/inf.png">-->

				<h1>A rede de compartilhamento de trabalhos do INF</h1>

				<!-- <h2><%= @project_count %> projetos registrados!</h2> -->

				<p>
					Teu programa do Neander ficou mais rápido que o do Weber? Tirou a nota mais alta da turma no relatório de Redes? Inventou uma classe que finalmente resolve o problema de usar texturas em OpenGL? Não deixa teus projetos sumirem na obscuridão das páginas pessoais da INF!
				</p>

				<p>
					No infHUB é fácil publicar teus melhores trabalhos, curtir os trabalhos dos teus colegas e até descobrir rapidamente qual foi o joguinho mais bacana que alguém já fez pra Projeto de Computação Gráfica.
					<%= link_to "Leia mais...", about_path, class: "no-decoration" %>
				</p>
			</div>
	    </div>
	    
	    <br>
	
		<div class="row-fluid">
			<center>
				<%= form_tag projects_path, method: 'get', class: "form-search" do %>
					<%= select_tag :course,  options_for_select(Course.all.collect {|p| ["#{p.name} (#{p.projects.size})", p.id] }), :include_blank => true , "data-placeholder" => "Escolha uma disciplina...", class: "chzn-select", :onchange => 'this.form.submit()' %>
					<!-- <%= submit_tag "Ir", class: "btn", name: nil, style: "margin-bottom: 20px;" %> -->
				<% end %>

				<div class="hdivisor">
					<div class="line"></div>
					<div class="txt">
						ou
					</div>
				</div>

				<!-- Search Form -->
				<%= render 'shared/search_form', destinyPath: projects_path, placeholder: "Procure um projeto por título/descrição/tag" %>

			</center>
		</div>

	</div>

	<div class="row-fluid">
		<div class="offset2 span4">
			<div class="news">
				<legend>Novos Projetos</legend>
				<ul>
					<% @lastProjects.each do |p| %>
						<li>
							<header><%= link_to_project p %></header>
							<p class="author">por <%= link_to p.person, profile_path(p.person.nick) %></p>
							<small>
								<p class="muted"><i class="icon-book"></i><%= p.course.name %></p>
								<p class="muted"><i class="icon-time"></i>há <%= time_ago_in_words(p.created_at) %></p>
							</small>
						</li>
					<% end %>
				</ul>
				<small>
					<%= link_to "Ver mais", projects_path(sort: "date", direction: "desc") %>
				</small>
			</div>

		</div>

		<div class="span4">
			<div class="news">
				<legend>Novos Usuários</legend>
				<ul>
					<% @lastPeople.each do |p| %>
						<li>
							<header><%= link_to p, profile_path(p.nick) %></header>
							<p class="author"><%= p.name %></p>
							<small>
								<p class="muted"><i class="icon-time"></i>há <%= time_ago_in_words(p.created_at) %></p>
							</small>
						</li>
					<% end %>
				</ul>
				<small>
					<%= link_to "Ver mais", people_path(sort: "date", direction: "desc") %>
				</small>
			</div>
		</div>
	</div>

	<br>
	<br>

</div>