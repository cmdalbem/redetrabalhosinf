<!-- Showing results for... -->
<%= render 'shared/search_result_title' %>

<!-- The content view -->
<% if @viewMode == :thumbs %>
	<%= render 'people_thumbs' %>
<% else %>
	<%= render 'people_list' %>
<% end %>

<!-- Pagination -->
<center>
  <%= will_paginate @projects %>
</center>