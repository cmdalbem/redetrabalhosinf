<!-- Showing results for... -->
<% if @hasQuery %>
	<%= render 'shared/search_result_title' %>
<% end %>

<!-- The content view -->
<% if @viewMode == :thumbs %>
	<%= render 'people/people_thumbs' %>
<% else %>
	<%= render 'people/people_list' %>
<% end %>

<!-- Pagination -->
<center>
	<%= will_paginate @people %>
</center>