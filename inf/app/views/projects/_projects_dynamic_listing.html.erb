<!-- Showing results for... -->
<% if @hasQuery %>
	<%= render 'shared/search_result_title' %>
<% end %>

<!-- The content view -->
<% if @projects.empty? %>
	<br>
	Sem trabalhos a exibir.
	<br>
	<br>
<% else %>
	<% if @viewMode == :list %>
	  <%= render 'projects/projects_table' %>
	<% else %>
	  <%= render 'projects/projects_thumbs' %>
	<% end %>
<% end %>

<!-- Pagination navigation -->
<%= render "shared/pages_nav", items: @projects %>