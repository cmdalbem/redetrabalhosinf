<!-- Showing results for... -->
<%= render 'shared/search_result_title' %>

<!-- The content view -->
<% if @viewMode == :list %>
  <%= render 'projects/projects_list' %>
<% else %>
  <%= render 'projects/projects_thumbs' %>
<% end %>

<!-- Pagination -->
<center>

</center>