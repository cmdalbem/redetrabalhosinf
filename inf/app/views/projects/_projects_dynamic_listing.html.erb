<!-- Showing results for... -->
<%= render 'shared/search_result_title' %>

<!-- The content view -->
<% if @projects.empty? %>
<center>
  <br>
  Sem projetos a exibir.
  <br>
  <br>
</center>
<% else %>
	<% if @viewMode == :list %>
	  <%= render 'projects/projects_list' %>
	<% else %>
	  <%= render 'projects/projects_thumbs' %>
	<% end %>
<% end %>

<!-- Pagination -->
<center>
	<%= will_paginate @projects%>
</center>