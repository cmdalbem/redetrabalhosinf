<h1> Show tweet </h1>

<p>
  <b><%= @tweet.person.name %></b>: <%= @tweet.text %>  
</p>

<br />

<h3> Comments </h3>
<p>
	<% if(@tweet.comments.empty?) %>
	No comments.
	<% else %>
	<%= render @tweet.comments %>
	<% end %>
</p>

<p>
	<h4>Leave a comment:</h4>
	<%= form_for([@tweet, @tweet.comments.build]) do |f| %>
		<div class="field">
			<%= f.label :person %><br />
			<%= f.select :person, Person.all.collect {|p| p.name } %>
		</div>
		<div class="field">
			<%= f.text_field :text %>
		</div>
		<div class="actions">
			<%= f.submit %>
		</div>
	<% end %>
</p>


<!--
<% @tweet.tweets.each do |t| %>
<p>
	<b><%= t.person.name %>:</b>
	<%= t.text %>
	<br />  
</p>
<% end %>
-->

<!--
<h3>Reply:</h3>
<p>
	<%= form_for(@tweet) do |f| %>
		<div class="field">
			<%= f.select :person, Person.all.collect {|p| p.name } %>
		</div>

		<div class="field">
			<%= f.label :text %><br />
			<%= f.text_field :text %>
		</div>

		<div class="actions">
			<%= f.submit "Submit Reply" %>
		</div>
	<% end %>
</p>
-->


<%= link_to 'Edit', edit_tweet_path(@tweet) %> |
<%= link_to 'Back', tweets_path %>
