<div id="likesModal" class="modal hide fade" tabindex="-1" role="dialog" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3>Quem curtiu</h3>
  </div>
  <div class="modal-body">
    <ol>
      <% @project.likes.each do |p| %>
        <li>
          <%= image_tag p.avatar.url(:small) %>
          <%= link_to p.name, profile_path(p.nick), class: "username-text" %>
        </li>
      <% end %>
    </ol>
  </div>
</div>