<!-- Showing results for... -->
<% if @hasQuery %>
	<%= render 'shared/search_result_title' %>
<% end %>

<br>

<!-- The content view -->
<% if @viewMode == :thumbs %>
	<%= render 'people/people_thumbs' %>
<% else %>
	<%= render 'people/people_table' %>
<% end %>

<!-- Pagination -->
<%= render "shared/pages_nav", items: @people %>