$("div#projects-table").html("<%= escape_javascript(render (@viewMode == :thumbs ? 'projects/projects_thumbs' : 'projects/projects_list'))%>");
<% if @viewMode == :thumbs %>
	$("#thumbs-view").addClass('active');
	$("#list-view").removeClass('active');
<% else %>
	$("#thumbs-view").removeClass('active');
	$("#list-view").addClass('active');
<% end %>